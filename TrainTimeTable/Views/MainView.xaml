<views:MvxWindowsPage
    x:Class="TrainTimeTable.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TrainTimeTable.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Cirrious.MvvmCross.WindowsCommon.Views"
    mc:Ignorable="d">

    <Hub>
        <HubSection Header="Любимые">
            <DataTemplate>
                <GridView ItemsSource="{Binding Favorites,Mode=TwoWay}">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding From.StationName}"/>
                                <TextBlock Text="{Binding To.StationName}"/>
                                <Image Width="200" Height="200" Source="{Binding To.Image.FullImageUrl}"/>
                            </StackPanel>
                           
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </DataTemplate>
        
       
        </HubSection>

        <HubSection Header="Новости">

        </HubSection>
    </Hub>



</views:MvxWindowsPage>
